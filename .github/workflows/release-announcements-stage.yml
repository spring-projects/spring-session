name: Release Announcements - Stage

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:
    inputs:
      version:
        description: The version to stage
        required: true
        type: string

permissions:
  contents: read

jobs:
  stage-release-announcements:
    name: Stage Release Announcements
    if: ${{ !github.event.repository.fork }}
    uses: spring-io/spring-security-release-tools/.github/workflows/release-announcements-stage.yml@d1199a03aee6023578d50c34ec6f09e37889672c # v1.0.19
    with:
      version: ${{ inputs.version || github.ref_name }}
    secrets: inherit
